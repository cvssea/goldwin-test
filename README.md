# Solution for GoldWin test

## Run locally

- `npm install`
- `npm run server`
- `npm start`

For logging in use the below preconfigured account:

- email: `john@doe.com` password: `secret`

The server side is simulated with `json-server`
